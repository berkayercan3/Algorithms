public class DİvideAndConquerAlgorithms {
}

/*
SumArray(A[] , sol, sag){
    orta = (sol + sag) / 2
    if sol == sag
        return sol

    solToplam = SumArray(A[] ,sol, orta)
    sagToplam = SumArray(A[] ,orta + 1 , sag)

    return solToplam + sagToplam
}
*/

/*
MultiplyArray(bas , son){
    if bas > son
        return 1
    if bas == son
        return start

    mid = (bas - son) / 2

    solCarpım = ( bas , mid )
    sagCarpım = ( orta + 1 , sag)

    return solCarpım * sagCarpım

}
 */

/*
Exponentiation(a, n){
    if n == 0
        return 1

    half = Exponentiation(a, n/2)

    if (n%2 == 0)
        return half * half
    else
        return a * half * half
}
 */




























